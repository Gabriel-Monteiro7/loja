import styled from 'styled-components'
import {} from 'styled-components'

export default styled.div`
    /* min-height: 100vh; */
    padding: 30px 0px 90px 0px;
    background-color: ${props=> props.background !== undefined ? props.background : ''}
`;